import { expect } from 'chai'
// import { APIPaymentToken } from './create_token'

describe('POST /api/v1/payments', () => {
  describe('APIPaymentToken', () => {
    expect('blah').to.equal('blah')
  })
})
